let uname="Sachin";
if(uname=="Sachin")
{
    console.log("Good Morning");
}

let k=10;
if(k%2==0)
    console.log(`${k} is Even number`);
else
    console.log(`${k} is odd`);

    let a=10;
    let b=30;
    let c=20;

    if(a>b)
    {
        if(a>c)console.log(`${a} is Max`);
        else console.log(`${c} is Max`);
    }
    else
    {
        if(b>c)console.log(`${b} is Max`);
        else console.log(`${c} is Max`);

    }